# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but DO commit the no-secrets template:
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Membrane: V4alpha / V4a / SingularWeb planning docs are governed outside this
# repo and must never enter its git history (kept on disk only).
v4alpha-*.md
singularweb-*.md
harvest-audit-prompt.md
PPC_Mastery_Project_Report_v3_June2026.md
