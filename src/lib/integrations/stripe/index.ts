// Stripe integration seam — Phase 2 (subscription collection).
// All Stripe calls live behind this module so the rest of the app imports a
// function, not the SDK. Intentionally empty until Phase 2.
export {};
