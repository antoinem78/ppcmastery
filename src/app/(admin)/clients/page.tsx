// Client list (stub). Real list/table comes once we have clients to show.

export default function ClientsPage() {
  return (
    <div className="p-10">
      <h1 className="text-2xl font-semibold text-zinc-900">Clients</h1>
      <p className="mt-1 text-sm text-zinc-500">
        The client book lives here. Listing UI arrives with the onboarding wizard
        (Phase 1).
      </p>
    </div>
  );
}
